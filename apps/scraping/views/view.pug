extends ../../../views/layout

block styles
  style.
    @page {
      size: A4;
      margin: 17mm;
    }

block content
  .page-header
    h2= scrap.name

  .hidden-print
    .btn-group.btn-group-justified
      a.btn.btn-primary(href="/scraping/edit/" + scrap._id) Edit
      a.btn.btn-danger(href="/scraping/delete/" + scrap._id) Delete

    hr

    .btn-group.pull-right
      button.btn.btn-default.btn-print(type="button")
        i.fa.fa-print
      a.btn.btn-default(href="/scraping/" + scrap._id + "/pdf")
        i.fa.fa-file-pdf-o
    .clearfix

    hr

  div.columns-sm-3.columns-print-2
    each el in html
      div !{el}

block scripts
  script(src="/scraping/js/main.js")